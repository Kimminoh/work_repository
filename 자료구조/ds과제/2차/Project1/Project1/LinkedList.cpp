#include <iostream>
#include <string>
using namespace std;

class LinkedList {
	int data;
	LinkedList* next;//Linked List라는 next 포인터

	void addNode(int a) {
		LinkedList* newnode = new LinkedList;
		data = a;
		newnode->data;//매개변수로 받은 정수데이터를 다음 
		newnode->next = NULL;

		if()

	}
};



int main() {
	LinkedList* head = NULL;
	LinkedList* tail = NULL;



	LinkedList * newNode = new LinkedList();
	newNode->


}